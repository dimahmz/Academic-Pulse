package com.example.academicpulse.theme

import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.ui.graphics.Color

val green = Color(0xFF0B8AA0)
val blue = Color(0xFFEAF2FF)
val white = Color(0xFFFFFFFF)

val danger = Color(0xFFD70000)
val brown = Color(0xFF655D53)
val primaryBackground = Color(0xFFFFFFFF)
val secondaryBackground = Color(0xFFEAF2FF)
val primary = Color(0xFF0B8AA0)
val borderColor = Color(0xFFC5C6CC)
val fontsColorHeader = Color(0xFF131314)
val fontsColorParagraph = Color(0xFF2F3036)
val fontsColorText = Color(0xFF71727A)

val inputGray = Color(0xFFCCCCCC)

val DarkColorScheme = darkColorScheme(
	primary = green,
	secondary = blue,
	tertiary = white,
	error = danger,
)

val LightColorScheme = lightColorScheme(
	primary = green,
	secondary = blue,
	tertiary = white,
	error = danger,
)
